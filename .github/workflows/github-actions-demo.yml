name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: make .ssh folder
        run: mkdir ~/.ssh
      - name: Add known hosts file
        run: ssh-keyscan -H 209.222.30.54 >> ~/.ssh/known_hosts
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_B00005555_PRIVATE }}
          known_hosts: ${{ secrets.SSH_HOST }}
      - name: Deploy with rsync
        run: ssh B00005555@209.222.30.54 './deploy.sh https://eoinok:mHni100wd@github.com/eoinok/tc30.git'
